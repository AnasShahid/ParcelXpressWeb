@model IEnumerable<DRVR_DATA>

@{
    ViewBag.Title = "AllDrivers";
}

<div class="container" style="margin-top:3%;">

    <div class="panel panel-default box box-danger">
        <div class="panel-heading">
            <h3>Drivers</h3>
        </div>

        <div class="panel-body">
            <div class="container-fluid box-body">
                <div class="row">
                    <div class="box-header">
                        <h4>Registered Drivers</h4>
                    </div>
                </div>

                <div class="row" style="margin-bottom:1%;">
                    <form method="get" action="@Url.Action("AllDriversAccounts")" data-pxp-ajax="true" data-pxp-target="#registeredDrivers">
                        <div class="col-xs-7 col-sm-5 col-md-3">
                            <div class="input-group col-sm-12">
                                <input type="search" class="form-control" placeholder="Search&hellip;" name="searchTerm" />
                                <span class="input-group-btn">
                                    <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-search"></span> </button>
                                </span>
                            </div>
                        </div>
                    </form>
                </div>

                @Html.Partial("_DriversTableAccounts", Model)
            </div>

        </div>
    </div>
</div>
@Html.Raw(TempData["toastMessage"])
